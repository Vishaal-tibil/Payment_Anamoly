# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtual environment
.venv/

# Test artifacts
.pytest_cache/

# Tool-internal state, not project code
.claude/

# Transient SQLite journal (data/payments.db itself IS committed --
# it ships with real resolved Meridian data + Track A snapshots
# already computed, so the team can pull and work immediately)
data/*.db-journal
